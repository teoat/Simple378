# Additional dependencies for production hardening
# Add these to your existing requirements.txt or pyproject.toml

# Prometheus metrics
prometheus-client>=0.19.0

# Redis async client (if not already included)
redis[hiredis]>=5.0.0

# For production deployment
uvicorn[standard]>=0.25.0
